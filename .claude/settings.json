{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "test -f scripts/precommit_gate.py || exit 0; for p in venv/Scripts/python.exe venv/bin/python; do [ -x \"$p\" ] && exec \"$p\" scripts/precommit_gate.py; done; exec python3 scripts/precommit_gate.py",
            "if": "Bash(git commit*)",
            "timeout": 200,
            "statusMessage": "Проверяю перед коммитом (ruff + тесты)"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "test -f scripts/test_edit_reminder.py || exit 0; for p in venv/Scripts/python.exe venv/bin/python; do [ -x \"$p\" ] && exec \"$p\" scripts/test_edit_reminder.py; done; exec python3 scripts/test_edit_reminder.py",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
