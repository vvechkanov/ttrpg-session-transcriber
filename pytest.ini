[pytest]
testpaths = tests
addopts = -v
markers =
    slow: marks tests as slow (requires GPU/ASR model, excluded from CI default run)
    requires_asr: marks tests that need a downloaded ASR model (faster-whisper or whisperx)
