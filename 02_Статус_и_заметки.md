# ttrpg-session-transcriber — Статус и заметки

**Обновлено:** 2026-06-12 (архитектурное ревью).

## Что уже стабильно

- Шестислойная архитектура (`domain / sources / mergers / renderers / core / ui`) — внедрена, см. `ARCHITECTURE.md`.
- GUI на PySide6/QML (`python -m ui`): таймлайн, волны, фазы пайплайна, настройки.
- ASR-бэкенды: faster-whisper (default), GigaAM-v3 (sherpa-onnx), WhisperX (legacy).
- Несколько Craig-архивов в одной сессии (per-segment ASR + waveforms).
- Редактор speaker_map (игрок → персонажи, ГМ, слушатель) + CastStrip.
- Склейка в `merged.txt` с интерливингом FVTT-чата; нарезка на LLM-чанки с overlap.
- CLI с паритетом GUI (`ui/cli.py`).
- ~440 тестов, CI-матрица (ubuntu/windows × py3.11/3.12), 3-уровневая e2e-стратегия.

## Что в процессе / на очереди

- Синхронизация документации и фундамент (pyproject, ruff, кэш ASR, единый реестр бэкендов) — Фазы 0–1 в `TASKS.md`.
- Фича #3 итерация 3b: пересчёт окна оси по реальным длительностям (абсолютная ось и парсинг боёв уже сделаны).
- Фича #6: LLM-мержер (спроектирован, поэтапно 6.1 → PoC → интеграция).

## Заморожено

- Инсталлер (#1): bootstrap .exe живёт в `launcher/`, не пересобирался после QML-правок. Не трогаем до явной разморозки.

## Где что лежит

- **Продуктовый бэклог:** `FEATURE_REQUESTS.md` (фичи #1–#9). Статусы сторожит `tests/test_feature_statuses.py`: у всех девяти — что заголовок и строка статуса не расходятся между собой; с кодом сверены #3, #4, #7, #8 — у них назван артефакт, который обязан лежать в дереве. Остальные пять на код не проверял никто, и «достоверны» про них означает только внутреннюю непротиворечивость.
- **Инженерный план:** `TASKS.md` (фазы 0–3, обновлён 2026-06-12).
- **Архитектурное ревью:** `docs/architecture-review-2026-06.md` (находки F-*, риски, обоснования).
- **Целевая архитектура:** `ARCHITECTURE.md` (актуализация запланирована — задача 1.6).
