# ttrpg-session-transcriber — Статус и заметки

**Обновлено:** 2026-08-03 (сведение трёх линий разработки в master + ревизия документации).

## Где что лежит

| Документ | О чём | Когда открывать |
|---|---|---|
| [VISION.md](VISION.md) | ТЗ: зачем инструмент, принципы, слои и слоты, лестница ступеней, границы | Спор о том, делать ли вообще |
| [TASKS.md](TASKS.md) | Инженерный план: ступени, разложенные в задачи, статус каждой | «Что делать дальше» |
| [FEATURE_REQUESTS.md](FEATURE_REQUESTS.md) | Бэклог фич #1–#9 с историей итераций | Подробности по конкретной фиче |
| [ARCHITECTURE.md](ARCHITECTURE.md) | Слои, контракты, ADR | Правка кода, новый бэкенд |
| [CHANGELOG.md](CHANGELOG.md) | Что менялось между версиями | Релиз |
| [docs/architecture-review-2026-08.md](docs/architecture-review-2026-08.md) | Прогон теста «слоёв достаточно» из ТЗ по коду (`F-8/*`) | Планирование Ступени 1 и дальше |
| [docs/architecture-review-2026-06.md](docs/architecture-review-2026-06.md) | Внешнее ревью июня (`F-*`) | Исторический контекст задач фундамента |
| [docs/design/screen-3-session.md](docs/design/screen-3-session.md) | Почему UI выглядит так, как выглядит | Правка экранов |
| [00_README.md](00_README.md) / [01_Как_пользоваться.md](01_Как_пользоваться.md) | Инструкции для пользователя (RU) | Запуск, обработка сессии |

## Где мы на лестнице

**Ступень 0 «Впервые не хуже старого» — условие готовности выполнено** (2026-08-02,
измерено на Сессии 15: 18 мин 34 с против 2–3 часов, +722 слова, 0 галлюцинаций против
39). Остался хвост: пересчёт окна таймлайна по реальной длительности, абсолютные часы на
линейке, доковый и инженерный долг — `TASKS.md` §3–§4.

**Следующая — Ступень 1 «Каркас, паспорта, рекомендация»:** каталог перестаёт быть
списком из двух захардкоженных бэкендов. Наполовину написана (установка модулей, экран
каталога), не хватает паспортов модулей, профиля железа и кнопки «Рекомендуемое».

## Что уже стабильно

- Шестислойная архитектура (`domain / sources / mergers / renderers / core / ui`).
- GUI на PySide6/QML (`python -m ui`): таймлайн, волны, фазы пайплайна, каталог моделей,
  настройки. CLI с паритетом (`ui/cli.py`).
- ASR-бэкенды: faster-whisper (default), GigaAM-v3 (sherpa-onnx), WhisperX (legacy —
  судьба не решена, `TASKS.md` §3.6).
- Несколько Craig-архивов в одной сессии (per-segment ASR + waveforms).
- Редактор speaker_map (игрок → персонажи, ГМ, слушатель) + CastStrip.
- Склейка в `merged.txt` с интерливингом FVTT-чата и типизированных событий боя;
  нарезка на LLM-чанки с overlap.
- Кэш дорожек: повторный прогон по той же сессии не гоняет ASR заново.
- ~490 тестов, CI-матрица (ubuntu/windows × py3.11/3.12), 3-уровневая e2e-стратегия.

## Известные дефекты

- **Окно таймлайна считается по дефолтным 4 часам**, а не по реальной длине дорожек:
  все полосы источников уезжают (~4 п.п. на Сессии 15). Только визуально — `merged.txt`
  не затронут. `TASKS.md` §3.1.
- **Линейка в относительных минутах** вместо абсолютных часов. `TASKS.md` §3.2.
- **Конвейеров два**: QML-оболочка не зовёт `core.pipeline.run`, а собирает всё сама
  (`ui/engines/merger_worker.py`). Правки в один путь молча не доезжают во второй — так
  фикс склейки 1.0 → 2.0 с не доехал до интерфейса (исправлено 2026-08-03, причина
  осталась). `TASKS.md` §4.3, ревью `F-8/5`.
- **Событие неизвестного типа исчезает молча** из выдачи: `renderers/plain_text.py` —
  `if/elif` без `else`. `TASKS.md` §3.4, ревью `F-8/12`.
- `core/cache.py` — мёртвый стаб эпохи P2 (`NotImplementedError`), кэш реализован в
  другом месте. `TASKS.md` §3.5.
- Ruff- и mypy-гейтов нет, хотя решение о ruff принято, а `ARCHITECTURE.md` §5.3 обещает
  exhaustiveness-проверку union'ов. `TASKS.md` §4.2.

## Заморожено

- **Инсталлер как продукт** (#1): онбординг, обновления, подпись — Ступень 5.
  Но **способность собраться** (CI собирает exe и запускает вхолостую) нужна начиная со
  Ступени 1 и постоянно: `release.yml` сейчас срабатывает только по тегу `v*`, тегов нет,
  последние правки UI в собранную сборку не попали. `TASKS.md` §5.5.
